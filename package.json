{
  "name": "worksheets-generator",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @wg/server --filter @wg/web dev",
    "dev:server": "pnpm --filter @wg/server dev",
    "dev:web": "pnpm --filter @wg/web dev",
    "build": "pnpm -r build",
    "start": "pnpm -r --parallel --filter @wg/server --filter @wg/web start",
    "start:server": "pnpm --filter @wg/server start",
    "start:web": "pnpm --filter @wg/web start",
    "test": "pnpm -r test"
  },
  "author": "GyGaByyyTe",
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "eslint": "^9",
    "eslint-config-next": "15.3.2",
    "eslint-config-prettier": "^10",
    "eslint-plugin-prettier": "^5",
    "prettier": "^3.5.3"
  }
}
